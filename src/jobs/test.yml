description: >
  Runs tests for a project

executor:
  name: docker_membrane
  mix_env: test

steps:
  - checkout
  - get_mix_deps
  - use_build_cache:
      env: test
  - run: mix compile
  - run: mix test --warnings-as-errors
