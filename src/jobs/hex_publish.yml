description: >
  Publish the package to hex

executor:
  name: docker_membrane
  mix_env: test

steps:
  - checkout
  - get_mix_deps
  - run:
      name: Publish to hex.pm
      command: mix hex.publish --organization membraneframework --yes
